#include<stdio.h>
main()
{
	int a,b,c,m,n;
	scanf("%d",&a);
	scanf("%d",&b);
	scanf("%d",&c);
	m==(-b+(b*b-4*a*c)^1/2)/2*a;
	n==(+b+(b*b-4*a*c)^1/2)/2*a;
	switch(m=0)
	{
		case 1:printf("%d",m);
		printf("real and equal");break;
	}
	switch(n=0)
	{
		case 2:printf("%d",n);
		printf("real and different");break;
	}

}
